[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "repograph"
version = "0.1.0"
description = "Local-first codebase intelligence engine with CLI and terminal TUI."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
  { name = "Malay Chhatbar" }
]
dependencies = [
  "pydantic>=2.11,<3.0",
  "textual>=0.82,<1.0",
  "typer>=0.16,<1.0",
  "rich>=14.1,<15.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.4,<9.0",
]

[project.scripts]
repograph = "repograph.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["src/repograph"]

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = ["tests/fixtures"]

[tool.ruff]
line-length = 100
target-version = "py311"
